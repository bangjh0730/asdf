version: 2.1

orbs:
  pytest: thekevjames/pytest@0.0.60

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - run:
          command: ./test_codes/test.py test
          name: Test

workflows:
  main:
    jobs:
      - build-and-test

